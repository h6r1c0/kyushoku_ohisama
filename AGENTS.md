# 給食アプリの変更方針

- 作業前にmain最新版と既存の保存・移行処理を確認する。保存済みの値（0・空欄・未設定・削除を含む）を新しい初期値で上書きしない。
- 指定外の機能・デザイン・正常な計算を維持し、大規模な整理を同時に行わない。
- 機能・配置・文言・操作・計算を変えたら、同じ変更内で影響するヘルプも同期する。実装後の実UIを使い、古い操作や矛盾するサンプルを表示しない。
- `tests/regression.cjs` とGitHub Actionsのスマホ操作テストを通す。320px・375px・768pxの画像を確認し、はみ出し・文字の重なり・操作対象の隠れがないことを確かめる。
- 完成済み画像は再生成・再着色しない。公開が依頼された場合はmainとGitHub Pagesの反映を確認する。
